# Docker

This directory is reserved for environment-specific Dockerfiles and compose
overrides used in later phases (e.g. GPU images, LangGraph workers). The
foundation phase uses the root `Dockerfile` and `docker-compose.yml`.
